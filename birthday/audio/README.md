# Birthday Audio

Place your audio file here as `happy-birthday.mp3`.

The site references it at `./audio/happy-birthday.mp3`.

If the file is missing the player will gracefully reveal the Continue button automatically.
